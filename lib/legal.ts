/** Shared constants for the legal pages (/terms, /privacy, /refunds). */
export const LEGAL_BRAND = "FitTrack";

/** Set NEXT_PUBLIC_SUPPORT_EMAIL in production — the fallback is a placeholder. */
export const SUPPORT_EMAIL = process.env.NEXT_PUBLIC_SUPPORT_EMAIL ?? "support@fittrack.app";

export const LEGAL_LAST_UPDATED = "June 12, 2026";
